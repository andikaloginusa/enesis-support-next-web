# Allow install scripts for packages that need them during CI/build.
# sharp    : native binary compilation (required by Next.js image optimization)
# unrs-resolver: npm internal resolver postinstall hook
#
# `npm_config_allow_scripts` env var enables install scripts in CI environments
# without interactive approval prompts.
npm_config_allow_scripts=true
